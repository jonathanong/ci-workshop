{
  "name": "ci-workshop",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint": "npm run eslint && npm run stylelint",
    "eslint": "eslint --fix --cache .",
    "stylelint": "stylelint --fix --cache 'client/**/*.css' 'client-storybook/**/*.css'",
    "test": "npm run test-jsdom && npm run test-server",
    "test-jsdom": "jest --coverage --config jest.jsdom.config.json",
    "test-server": "jest --coverage --config jest.server.config.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
